import React from 'react';
import PropTypes from 'prop-types';

const Banner = props => {
    return (
        <div>
            <h2>Homepage Banner</h2>
        </div>
    );
};

Banner.propTypes = {
    
};

export default Banner;